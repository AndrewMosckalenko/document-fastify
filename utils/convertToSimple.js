export const simpleTag = (tag) => ({ title: tag.title, id: tag.id });
export const simpleProject = (project) => ({
  name: project.name,
  id: project.id,
});
export const simpleDocument = (document) => ({
  name: document.name,
  id: document.id,
});
